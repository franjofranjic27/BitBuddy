spring:
  application:
    name: market-data-service
  datasource:
    url: ${DATASOURCE_URL:jdbc:postgresql://localhost:5433/market_data}
    username: ${DATASOURCE_USERNAME:marketdata}
    password: ${DATASOURCE_PASSWORD:secret}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop   # oder validate/create-drop je nach Bedarf
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  kafka:
    admin:
      auto-create: true
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

server:
  port: ${SERVER_PORT:8080}
  servlet:
    context-path: /api

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,loggers
  endpoint:
    health:
      show-details: always

mds:
  crypto:
    pairs:
      - BTC/USDT
      - ETH/USDT
      - ADA/USDT

logging:
  level:
    info.bitrich.xchangestream.bitstamp.v2.BitstampStreamingService: ERROR